APP_NAME="Gestione Assenze"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://localhost:8080

APP_LOCALE=it
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=it_IT

APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=12
AUTH_LOGIN_MAX_ATTEMPTS=5
AUTH_LOGIN_MAX_ATTEMPTS_PER_IP=25
AUTH_LOGIN_DECAY_SECONDS=300
AUTH_FORGOT_PASSWORD_MAX_ATTEMPTS=6
AUTH_FORGOT_PASSWORD_DECAY_SECONDS=60
AUTH_RESET_PASSWORD_MAX_ATTEMPTS=6
AUTH_RESET_PASSWORD_DECAY_SECONDS=60

LOG_CHANNEL=stderr
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=gestioneassenze
DB_USERNAME=gestioneassenze
DB_PASSWORD=gestioneassenze_password

MARIADB_DATABASE=gestioneassenze
MARIADB_USER=gestioneassenze
MARIADB_PASSWORD=gestioneassenze_password
MARIADB_ROOT_PASSWORD=change_this_root_password

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="no-reply@gestione-assenze.local"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
